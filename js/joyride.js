$(document).ready(function() {
	//add an id to the add record button to make the tour work
	$('.jtable-toolbar-item-add-record').attr('id', 'addRecord');

	

	$(document).foundation('joyride', 'start');
});